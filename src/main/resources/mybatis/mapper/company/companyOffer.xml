<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.app.company.model.CompanyOfferMapper">

    <!-- 구직자 정보 조회하기 -->
    <select id="selectJobSeekerList"
            resultType="com.spring.app.company.domain.MemberSimpleDTO">
      SELECT
          MEMBERID AS memberId,
          NAME     AS name,
          EMAIL    AS email
      FROM TBL_MEMBER
      WHERE TYPE = 0
        AND STATUS = 1
      ORDER BY CREATED_AT DESC
    </select>


    <!-- =========================
         템플릿(원본) 기준 통계
         - 화면 상단 합계/목록용
         - 삭제된 원본(status=1)은 제외
       ========================= -->

    <!-- 발송한 제안서 전체 통계 조회하기 -->
    <select id="selectOfferMetricsSummary"
            resultType="com.spring.app.company.domain.OfferMetricsSummaryDTO">
      SELECT
        NVL(COUNT(r.fk_memberid), 0) AS sent,
        NVL(SUM(CASE WHEN r.viewed_at IS NOT NULL THEN 1 ELSE 0 END), 0) AS viewed,
        NVL(SUM(CASE WHEN r.response_status = 1 THEN 1 ELSE 0 END), 0) AS accepted,
        NVL(SUM(CASE WHEN r.response_status = 2 THEN 1 ELSE 0 END), 0) AS rejected
      FROM tbl_offer_letter ol
      LEFT JOIN tbl_offer_submit s
        ON s.fk_offer_letter_id = ol.offer_letter_id
      LEFT JOIN tbl_offer_response r
        ON r.fk_offer_submit_id = s.offer_submit_id
      JOIN tbl_job_posting jp
        ON jp.job_id = ol.fk_job_id
      WHERE jp.fk_memberid = #{companyMemberId}
        AND ol.status = 0
    </select>


    <!-- 발송한 제안서 상태 조회해오기 -->
    <select id="selectOfferMetricsByCompany"
        resultType="com.spring.app.company.domain.OfferMetricsDTO">
	  SELECT
	    ol.offer_letter_id AS offerLetterId,
	    NVL(COUNT(r.fk_memberid), 0) AS totalSent,
	    NVL(SUM(CASE WHEN r.viewed_at IS NOT NULL THEN 1 ELSE 0 END), 0) AS viewedCnt,
	    NVL(SUM(CASE WHEN r.fk_memberid IS NOT NULL AND r.viewed_at IS NULL THEN 1 ELSE 0 END), 0) AS unreadCnt,
	    NVL(SUM(CASE WHEN r.response_status = 1 THEN 1 ELSE 0 END), 0) AS acceptCnt,
	    NVL(SUM(CASE WHEN r.response_status = 2 THEN 1 ELSE 0 END), 0) AS rejectCnt
	  FROM tbl_offer_letter ol
	  LEFT JOIN tbl_offer_submit s
	    ON s.fk_offer_letter_id = ol.offer_letter_id
	  LEFT JOIN tbl_offer_response r
	    ON r.fk_offer_submit_id = s.offer_submit_id
	  JOIN tbl_job_posting jp
	    ON jp.job_id = ol.fk_job_id
	  WHERE jp.fk_memberid = #{companyMemberId}
	    AND ol.status = 0
	  GROUP BY ol.offer_letter_id
	  ORDER BY ol.offer_letter_id DESC
	</select>


    <!-- 제안서 리스트 조회해오기 : 템플릿 목록 -->
    <select id="selectOfferList"
            resultType="com.spring.app.company.domain.OfferListDTO">
      SELECT
          l.offer_letter_id AS offerLetterId,
          l.fk_job_id       AS jobId,
          l.title           AS title,

          (
            SELECT s.expire_at
            FROM tbl_offer_submit s
            WHERE s.fk_offer_letter_id = l.offer_letter_id
            ORDER BY s.send_at DESC
            FETCH FIRST 1 ROW ONLY
          ) AS expireAt,

          0 AS totalSent,
          0 AS viewedCnt,
          0 AS acceptCnt,
          0 AS rejectCnt,
          0 AS unreadCnt

      FROM tbl_offer_letter l
      JOIN tbl_job_posting j
        ON j.job_id = l.fk_job_id
      WHERE j.fk_memberid = #{companyMemberId}
        AND l.status = 0
      ORDER BY l.created_at DESC
    </select>


    <!-- 제안서 상세정보 조회해오기 : 템플릿 상세 -->
    <select id="selectOfferDetail"
	        parameterType="long"
	        resultType="com.spring.app.company.domain.OfferDetailDTO">
	  SELECT
	      l.offer_letter_id AS offerLetterId,
	      l.fk_job_id       AS jobId,
	      j.title           AS jobTitle,	/*공고의 제목*/
	      l.title           AS title,		/*제안서의 제목*/
	      l.message         AS message,
	      (
	        SELECT s.expire_at
	        FROM tbl_offer_submit s
	        WHERE s.fk_offer_letter_id = l.offer_letter_id
	        ORDER BY s.send_at DESC
	        FETCH FIRST 1 ROW ONLY
	      ) AS expireAt
	  FROM tbl_offer_letter l
	  LEFT JOIN tbl_job_posting j
	    ON j.job_id = l.fk_job_id
	  WHERE l.offer_letter_id = #{id}
	    AND l.status = 0
	</select>


    <!-- 제안서 등록하기 -->
    <insert id="insertOfferLetter"
            parameterType="com.spring.app.company.domain.OfferCreateRequestDTO">

      <selectKey keyProperty="offerLetterId" resultType="long" order="BEFORE">
        SELECT SEQ_TEST_OFFER_LETTER.NEXTVAL FROM dual
      </selectKey>

      INSERT INTO TBL_OFFER_LETTER
      (OFFER_LETTER_ID, FK_JOB_ID, TITLE, MESSAGE, CREATED_AT, UPDATE_AT, STATUS)
      VALUES
      (#{offerLetterId}, #{jobId}, #{title}, #{message}, SYSDATE, SYSDATE, 0)
    </insert>


    <!-- 제안서 수정하기 : 삭제되지 않은 원본만 -->
    <update id="updateOfferLetter"
            parameterType="com.spring.app.company.domain.OfferUpdateRequestDTO">
      UPDATE TBL_OFFER_LETTER
         SET FK_JOB_ID = #{jobId},
             TITLE = #{title},
             MESSAGE = #{message},
             UPDATE_AT = SYSDATE
       WHERE OFFER_LETTER_ID = #{offerLetterId}
         AND STATUS = 0
    </update>


    <!-- 발송 이력 존재 여부 -->
    <select id="existsOfferSendHistory" resultType="int">
      SELECT CASE WHEN EXISTS (
        SELECT 1
        FROM TBL_OFFER_SUBMIT s
        WHERE s.fk_offer_letter_id = #{offerLetterId}
      ) THEN 1 ELSE 0 END AS ok
      FROM dual
    </select>


    <!-- 원본 제안서 삭제하기 : 소프트 삭제 -->
    <update id="deleteOfferLetter">
      UPDATE TBL_OFFER_LETTER
         SET STATUS = 1,
             UPDATE_AT = SYSDATE
       WHERE OFFER_LETTER_ID = #{offerLetterId}
         AND STATUS = 0
    </update>


    <!-- 원본 소유권 확인 : 템플릿용 -->
    <select id="existsOfferLetterOwnedByCompany" resultType="int">
      SELECT CASE WHEN EXISTS (
        SELECT 1
        FROM TBL_OFFER_LETTER ol
        JOIN TBL_JOB_POSTING jp
          ON jp.JOB_ID = ol.FK_JOB_ID
        WHERE ol.OFFER_LETTER_ID = #{offerLetterId}
          AND jp.FK_MEMBERID = #{companyMemberId}
          AND ol.STATUS = 0
      ) THEN 1 ELSE 0 END AS ok
      FROM dual
    </select>


    <!-- 발송 기록 소유권 확인 : 기록용
         원본 status와 무관하게 확인 가능해야 함 -->
    <select id="existsOfferHistoryOwnedByCompany" resultType="int">
      SELECT CASE WHEN EXISTS (
        SELECT 1
        FROM TBL_OFFER_SUBMIT s
        JOIN TBL_JOB_POSTING jp
          ON jp.JOB_ID = s.FK_JOB_ID
        WHERE s.FK_OFFER_LETTER_ID = #{offerLetterId}
          AND jp.FK_MEMBERID = #{companyMemberId}
      ) THEN 1 ELSE 0 END AS ok
      FROM dual
    </select>


    <!-- 제안서 발송 => 스냅샷 insert -->
    <insert id="insertOfferSubmitSnapshot" parameterType="map">
      <selectKey keyProperty="offerSubmitId" resultType="long" order="BEFORE">
        SELECT SEQ_OFFER_SUBMIT_ID.NEXTVAL FROM dual
      </selectKey>

      INSERT INTO TBL_OFFER_SUBMIT (
        OFFER_SUBMIT_ID,
        FK_OFFER_LETTER_ID,
        FK_JOB_ID,
        TITLE,
        MESSAGE,
        EXPIRE_AT,
        SEND_AT
      )
      SELECT
        #{offerSubmitId},
        ol.OFFER_LETTER_ID,
        ol.FK_JOB_ID,
        ol.TITLE,
        ol.MESSAGE,
        #{expireAt, jdbcType=DATE},
        SYSDATE
      FROM TBL_OFFER_LETTER ol
      WHERE ol.OFFER_LETTER_ID = #{offerLetterId}
        AND ol.STATUS = 0
    </insert>


    <!-- 제안서 발송 => response 다건 insert -->
    <insert id="insertOfferResponses">
      INSERT INTO TBL_OFFER_RESPONSE (
        FK_OFFER_SUBMIT_ID,
        FK_MEMBERID,
        RESPONSE_STATUS
      )
      <foreach collection="receiverMemberIds" item="mid" separator=" UNION ALL ">
        SELECT #{offerSubmitId}, #{mid}, 0 FROM dual
      </foreach>
    </insert>


    <!-- 제안서를 발송한 회원(memberId) 목록 조회 : 기록 기준 -->
    <select id="selectSentMemberIdsByOfferLetterId"
            parameterType="long"
            resultType="string">
        SELECT DISTINCT r.fk_memberid
        FROM tbl_offer_response r
        JOIN tbl_offer_submit s
          ON s.offer_submit_id = r.fk_offer_submit_id
        WHERE s.fk_offer_letter_id = #{offerLetterId}
    </select>


    <!-- 제안서 수신자 상세 조회 : 기록 기준 -->
    <select id="selectOfferRecipientDetailsByOfferLetterId"
            parameterType="long"
            resultType="com.spring.app.company.domain.OfferRecipientDetailDTO">
        SELECT
            s.fk_offer_letter_id AS offerLetterId,
            s.offer_submit_id    AS offerSubmitId,
            s.title              AS offerTitle,

            r.fk_memberid        AS memberId,
            m.name               AS receiverName,
            m.email              AS receiverEmail,

            s.send_at            AS sendAt,
            r.viewed_at          AS viewedAt,
            r.responded_at       AS respondedAt,

            r.response_status    AS responseStatus,

            CASE
                WHEN r.response_status = 1 THEN '수락'
                WHEN r.response_status = 2 THEN '거절'
                ELSE '미응답'
            END AS responseStatusText

        FROM tbl_offer_submit s
        JOIN tbl_offer_response r
          ON r.fk_offer_submit_id = s.offer_submit_id
        LEFT JOIN tbl_member m
          ON m.memberid = r.fk_memberid
        WHERE s.fk_offer_letter_id = #{offerLetterId}
        ORDER BY s.send_at DESC, r.fk_memberid ASC
    </select>


    <!-- 제안서 발송 가능 여부 검사용 공고 정보 조회 : 템플릿 기준 -->
    <select id="selectOfferSendValidationInfo"
            resultType="com.spring.app.company.domain.OfferSendValidationDTO">
      SELECT
          ol.offer_letter_id AS offerLetterId,
          ol.fk_job_id       AS jobId,
          jp.fk_memberid     AS companyMemberId,
          jp.status          AS jobStatus,
          jp.opened_at       AS openedAt,
          jp.deadline_at     AS deadlineAt,
          jp.closed_at       AS closedAt
      FROM tbl_offer_letter ol
      JOIN tbl_job_posting jp
        ON jp.job_id = ol.fk_job_id
      WHERE ol.offer_letter_id = #{offerLetterId}
        AND ol.status = 0
    </select>


    <!-- 수신자가 유효한지 검사 조회 -->
    <select id="countValidOfferReceivers" resultType="int">
      SELECT COUNT(*)
      FROM tbl_member
      WHERE type = 0
        AND status = 1
        AND memberid IN
        <foreach collection="receiverMemberIds" item="mid" open="(" separator="," close=")">
          #{mid}
        </foreach>
    </select>


    <!-- 이미 발송한 회원 재확인 : 기록 기준 -->
    <select id="selectAlreadySentReceiverIds"
            resultType="string">
      SELECT DISTINCT r.fk_memberid
      FROM tbl_offer_response r
      JOIN tbl_offer_submit s
        ON s.offer_submit_id = r.fk_offer_submit_id
      WHERE s.fk_offer_letter_id = #{offerLetterId}
        AND r.fk_memberid IN
        <foreach collection="receiverMemberIds" item="mid" open="(" separator="," close=")">
          #{mid}
        </foreach>
    </select>
    
    
    
    <!-- 삭제된 원본 제안서 중 발송 이력이 있는 목록 -->
	<select id="selectDeletedOfferHistoryList"
	        resultType="com.spring.app.company.domain.DeletedOfferHistoryDTO">
	  SELECT
	      ol.offer_letter_id AS offerLetterId,
	      ol.fk_job_id       AS jobId,
	      ol.title           AS title,
	
	      NVL(COUNT(r.fk_memberid), 0) AS totalSent,
	      NVL(SUM(CASE WHEN r.viewed_at IS NOT NULL THEN 1 ELSE 0 END), 0) AS viewedCnt,
	      NVL(SUM(CASE WHEN r.viewed_at IS NULL THEN 1 ELSE 0 END), 0) AS unreadCnt,
	      NVL(SUM(CASE WHEN r.response_status = 1 THEN 1 ELSE 0 END), 0) AS acceptCnt,
	      NVL(SUM(CASE WHEN r.response_status = 2 THEN 1 ELSE 0 END), 0) AS rejectCnt,
	      MAX(s.send_at) AS lastSendAt
	
	  FROM tbl_offer_letter ol
	  JOIN tbl_job_posting jp
	    ON jp.job_id = ol.fk_job_id
	  JOIN tbl_offer_submit s
	    ON s.fk_offer_letter_id = ol.offer_letter_id
	  LEFT JOIN tbl_offer_response r
	    ON r.fk_offer_submit_id = s.offer_submit_id
	  WHERE jp.fk_memberid = #{companyMemberId}
	    AND ol.status = 1
	  GROUP BY ol.offer_letter_id, ol.fk_job_id, ol.title
	  ORDER BY MAX(s.send_at) DESC
	</select>

</mapper>